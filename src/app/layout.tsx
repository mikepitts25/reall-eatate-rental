import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });

export const metadata: Metadata = {
  title: {
    default: "LeaseFlip — Guaranteed rent for owners, real deals for operators",
    template: "%s · LeaseFlip",
  },
  description:
    "LeaseFlip is the marketplace connecting property owners who want guaranteed monthly income with operators who lease and run properties for profit.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
